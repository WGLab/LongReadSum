name: longreadsum
channels:
  - bioconda
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - python
  - numpy
  - hdf5
  - ont_vbz_hdf_plugin
  - htslib
  - swig
  - plotly
  - pytest

# conda env create --file=environment.yml
# conda activate longreadsum
